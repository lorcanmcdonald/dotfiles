const React = require("react"),
  { propTypes, t } = require("tcomb-react");

const COMPONENT = (props) => (
  <span/>);

COMPONENT.displayName = "COMPONENT";
COMPONENT.propTypes = propTypes({});

module.exports = COMPONENT;
